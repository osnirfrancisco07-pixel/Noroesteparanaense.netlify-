eufunction toggleMenu() {
    const navLinks = document.getElementById('nav-links');
    // Adiciona ou remove a classe 'active' para mostrar/esconder o menu
    navLinks.classList.toggle('active');
}
